# 画像アセット（任意）

CPU 対戦の立ち絵は `alpha_*.png` / `beta_*.png` / `gamma_*.png`（各 `_normal` `_doubt` `_smug` `_shock` `_defeat`）をこのフォルダに置くと表示される。

ファイルが無い場合でもゲームロジックは動作する（CPU の画像枠はブラウザの壊れた画像表示になることがある）。

README 用の代表画像は `ogp.png`（未作成なら任意のスクショ PNG をこの名前で置く）。
